---
description: Show Beacon embedding index health — file count, chunk count, last sync
allowed-tools: [Bash]
---

# /index-status

Run `node ${CLAUDE_PLUGIN_ROOT}/scripts/status.js` and display the results.
Report: files indexed, total chunks, last sync time, embedding model, endpoint.
